<%t=Time.now %>
<center>  <H1 >Hello <%= current_student.name %></H1></center>
  <br />
  <div class = "wrapper">
  <p>
    Today is <%=@current_time.strftime("%A") %>, <%= @current_time.strftime("%B") %> <%= @current_time.strftime("%d") %>, <%= @current_time.strftime("%Y") %> <br />
  </p>

<form method="post" action="/students/<%= current_student.id%>/edit">
  <input id="hidden" type="hidden" name="_method" value="patch">

  <h2>Check off the homework you completed today:</h2>

      <% @subjects.all.each do |subject| %>
      <p><input id="<%= subject.id %>" type="checkbox" name=" subject_ids[]"  value =' <%= subject.id %>' <%='checked' if current_student.subjects.include?(subject) %>> <%=subject.name%> </input></p>

      <% end %>


  <h2>Check off lessons you go to study today:</h2>
      <% @lessons.all.each do |lesson| %>
        <p><input id="<%= lesson.id %>" type="checkbox" name="lesson_ids[]" value="<%= lesson.id %>" <%='checked' if current_student.lessons.include?(lesson) %>><%=lesson.name%> </input></P>

      <% end %>

    <p><input type="submit" name="Submit studylesson" value="Submit studylesson" /></p>
</form>
<br />
  <form method="get" action='/students/logout'>
  <p><input type='submit' id='signout' value='Sign Out' name='signout' /></p>
  </form>
<a href='/students/show'>Head to display page without making changes.</a>
</div>
